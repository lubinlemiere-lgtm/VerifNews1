// ###########################################################################
// # Profil — Ecran placeholder (le vrai profil est dans le ProfileDrawer)
// # Ce fichier doit exister car Expo Router le reference dans les tabs,
// # mais il est masque (href: null). Le drawer lateral gere tout.
// ###########################################################################

import { View } from "react-native";

export default function ProfileScreen() {
  return <View />;
}
